{% extends '_admin.base.html.twig' %}


{% block body %}
<h1>Uploading a new photo</h1>

{{ form_start(form) }}
    {# ... #}

    {{ form_widget(form) }}
{{ form_end(form) }}

    <button type="submit" class="btn btn-primary" formnovalidate>Save</button>

{% endblock body %}