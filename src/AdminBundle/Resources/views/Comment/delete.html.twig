{% extends '_admin.base.html.twig' %}

{% block body %}




    <div class="container">
        <div class="row">
            <div class="col-xs-12">
                <h3>Are you sure you want to delete comment: " {{ deleteForm.vars.value.comment }} "?</h3>
                {{ form_start(deleteForm) }}

                {{ form_errors(deleteForm) }}

                {{ form_errors(deleteForm) }}

                {{ form_end(deleteForm) }}
            </div>
        </div>
    </div>
{% endblock body %}